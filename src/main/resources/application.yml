server:
  port: 8084
spring:
  boot:
    admin:
      client:
        url: http://localhost:9092/
        
  application:
    name: USER-LOGIN
  datasource:
    password: Kiran*123#
    url: jdbc:mysql://localhost:3306/projects
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

    
management:
  endpoints:
    web:
      exposure:
        include: '*' 